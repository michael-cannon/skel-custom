# skel-custom Directory Customizations

Private `bin` customizations mostly useful for WordPress releases.

## Install 

```
cd ~/bin
git clone git@github.com:michael-cannon/skel-custom.git custom
```
